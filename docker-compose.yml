version: '3.8'
services:
  nanggroe-iot:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./db/nanggroe-iot.db
      - NODE_ENV=production
      - NANGGROE_API_KEY=${NANGGROE_API_KEY:?Set NANGGROE_API_KEY in your .env file before starting}
    volumes:
      - iot-data:/app/db
      - iot-uploads:/app/uploads
    restart: unless-stopped

volumes:
  iot-data:
  iot-uploads:
